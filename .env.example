# Optional. If set, /search uses Brave Search API for full web results.
# Without this key, the prototype falls back to DuckDuckGo Instant Answer
# and a prototype DuckDuckGo HTML result parser.
BRAVE_SEARCH_API_KEY=

# Optional. Defaults to 4180.
PORT=4180
